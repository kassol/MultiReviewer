# MultiReviewer

对 pull request 执行多模型并行代码审查的工具。目标平台是公司内部 self-host 的 Gitea,开发阶段以 GitHub 为测试平台。

## Language

**Forge**:
托管代码与 pull request 的平台。当前有 Gitea 与 GitHub 两个实现,接口以 Gitea 的能力为基准。
_Avoid_: 平台、代码托管、Git 服务

**Review Run**:
一次审查执行。针对某个 pull request 的某个 head commit,运行一组 Reviewer,输出一批 Finding。同一 pull request 的每个新 head commit 触发新的 Review Run。pull request 触发的每一轮记下开跑时那个 pull request 的标题;范围审查触发的不记,那个阶段的名字来自范围审查自身。
_Avoid_: 审查任务、review job、扫描

**Review Range**:
一次 Review Run 覆盖的代码范围,由 base 与 head 两个 commit 界定。PR 触发时取 pull request 的 base 与 head,范围审查时 base 是阶段基准,head 是当时的比较项。
_Avoid_: diff、变更集

**范围审查**:
由人在面板发起的一个阶段性审查。它以一个 base commit 为该阶段不变的基准,以比较项为当前被审的代码;比较项每推进一次就跑一轮 Review Run。base 与比较项都可从分支的提交历史或 Tag 中选定;Tag 只帮助人找到 commit,范围审查固定到选定时所指的 commit,不随 Tag 后续移动。范围不必对应任何既有 pull request,直推默认分支的代码也可审。发起时由人给一个标题,作用等同 pull request 的标题,发起后不可改。每个范围审查有自己的标识,不按 base 去重;同一仓库同一 base 已有未完成的范围审查时只提醒。由人标记审查完成而进入终态,完成后再次发起是新的范围审查。它与 pull request 一样是审查阶段的一种来源,在评审记录里以同一形态出现,没有自己的页面。
_Avoid_: 手动审查、自定义评审、commit 审查、长期审查

**比较项**:
一个范围审查当前被审的 commit。发起时由人从仓库分支的提交历史或 Tag 中选定,之后随作者迭代由人在面板以同样方式推进;任一时刻的比较项都必须是 base 的后代,不要求是上一个比较项的后代。增量评审即发起该范围审查的新一轮 Review Run,并同步容器 PR 的 head。面板记下选定它时用的分支或 Tag,只用于下次打开选择器。
_Avoid_: head、目标 commit、当前版本

**审查完成**:
人对一个范围审查作出的终态标记。标记后容器 PR 关闭、其分支删除,比较项不再推进,未处置的 Finding 按未处置计入处置率。PR 触发的 Review Run 没有这个动作,它的终态来自 pull request 本身的关闭。
_Avoid_: 关闭审查、归档、结束

**处置备注**:
人在面板处置一条 Finding 时附的一句话,只存在于面板,不写入 Forge。一条 Finding 至多一条,可选。
_Avoid_: 回复、评论、反馈

**容器 PR**:
MultiReviewer 为一个范围审查在 Forge 上自建的 pull request,base 分支指向阶段基准,head 分支跟随比较项,只用来承载行内 Finding 与 Disposition,永不合并。它对面板用户透明,只在开发层面存在;Forge 上由固定的分支前缀与标题前缀识别,它产生的 webhook 事件不触发 Review Run。
_Avoid_: 临时 PR、审查 PR、机器人 PR

**Reviewer**:
一个绑定了具体模型的审查执行体。一次 Review Run 并行运行多个 Reviewer。
_Avoid_: agent、模型、worker

**Finding**:
一条被提出的代码问题。归属于提出它的 Reviewer,并指向 Review Range 内的具体位置。
_Avoid_: issue、问题、comment、告警

**Finding Identity**:
判断两条 Finding 是不是同一条的依据,中文叫「同一处 Finding」。同一个 pull request 里指向同一处未改动代码的 Finding 是同一条,不论由哪个或多少个 Reviewer 报出、在多少轮 Review Run 里被报出;同一轮内多个 Reviewer 报同一处合成一条,归属记全部报出它的 Reviewer。代码改动后经复核判定仍在的,由延续保持同一条;未经复核而在改动后的代码上再次报出的是新的一条。折叠到历史 Finding 上的那条落库时沿用历史行的指纹,同一条 Finding 在阶段汇总里因此跨轮次只占一个 Identity。
_Avoid_: 去重、指纹、逻辑 Finding、按模型各算一条

**参与条数**:
一个模型在处置率统计里的唯一一列辅助数字:该模型报出过的 Finding Identity 数,同一条 Finding 不论被它在几轮里报出都只计一次。它只说明这个模型有没有在干活,不构成处置率,也不是模型质量评分(ADR 0015)。
_Avoid_: 模型处置率、模型得分、报出数

**审查阶段**:
处置与统计的单位,也是评审记录的单位。一个范围审查从发起到审查完成是一个审查阶段;一个 pull request 从打开到关闭也是一个审查阶段,关闭后重开延续同一阶段。阶段只有进行中与已结束两种状态。阶段内所有轮次的 Finding 按 Finding Identity 汇总为阶段的当前状态,轮次只是它的历史,在阶段详情里按时间线呈现。
_Avoid_: 批次、轮次、Review Run

**评审记录**:
面板里一个审查阶段的条目。列表只有一份,它就是首页,以审查阶段为行,pull request 与范围审查同列同形,以来源标记区分;左栏是当前账号可见的仓库,它是这份列表的过滤条件,不是第二份列表。
_Avoid_: 运行记录、审查列表、run 列表

**复核**:
新一轮 Review Run 中 Reviewer 对本审查阶段每条未处置的历史 Finding 给出的结论,三选一:仍在、已修、无法判断。漏给结论按无法判断计。多个 Reviewer 结论冲突时仍在优先,全部已修才算已修。
_Avoid_: 重新审查、验证、确认修复

**Disposition**:
对一条 Finding 的处置结论。载体是 Forge 上该条 review 评论的 resolve 状态,已 resolve 即已处置。人在面板或 Forge 上作出的是人工处置;新一轮 Review Run 中复核判定已修时自动 resolve,记为「已修复」,与人工处置分开统计。自动处置不覆盖人已作出的处置;人把「已修复」改回未处置后它就是人工处置。它是判断审查质量的唯一信号来源。
_Avoid_: 反馈、评分、标注、已改动

**已延续**:
一条 Finding 经复核判定仍在、但所指代码已改动时,旧位置的评论所进入的状态:旧评论 resolve,新位置的评论承接同一条 Finding Identity。它只是位置的交接,不是处置,不计入处置率。交接以新评论的发布确认为准:新评论发出并读回标识之后才关旧评论、记延续。旧评论没关上的那次延续带「交接未完成」——它是「已延续」之上的一个待办标记,旧评论仍留在 Forge 上可处置,下一轮 Review Run 收尾时重试关闭,回填读到它已关闭也算交接完成。
_Avoid_: 已迁移、已转移、已处置、交接失败

**审查轨迹**:
一轮 Review Run 里按时间顺序发生的事件序列。每个 Reviewer 有自己的轨迹:它说过的话、调过的工具与参数、报出与被拒的 Finding;Review Run 自身的编排事件(工作副本就绪、批次起止、Finding 合并、评论已发、Reviewer 失败、轮次失败)也是轨迹。轨迹随 Review Run 保存,运行中可实时看,结束后可回看;能看该轮次的人就能看它的轨迹。它记的是过程,处置与统计不读它。
_Avoid_: 日志、transcript、agent 输出、会话记录

**取证**:
Reviewer 在报出 Finding 前对一条因果主张发起的一次只读调查:一个子代理沿调用链核查主张,带 file:line 证据返回报告,由 Reviewer 裁决是否报出。取证的阅读半径不限于变更文件;Finding 的落点仍收敛于 Review Range 的变更行内。
_Avoid_: subagent、子任务、调查、验证

**本轮指令**:
发起重审时评审方附的一次性要求,只作用于那一轮 Review Run,下一轮不带。要持续生效的要求应沉淀为知识条目。
_Avoid_: 临时指令、评审备注、附加提示

**续跑**:
服务重启打断一轮 Review Run 之后,下次启动把这一轮接着跑完:已经跑完的批次沿用已落库的结果,只跑缺结果的那些,再走同一段合并、收尾与发评论。它沿用原轮次的编号与轨迹,不是新的一轮。续跑的前提是这一轮的冻结事实没有变过——同一个 head、同一版知识集、重新切批与开跑时冻结的完整批次计划一致(已完成与未完成的批次都要相符)、模型组合还是那几个;任一条不成立就不续跑,那一轮改判失败并说明原因;原因记在轮次自己上(见「轮次失败」),同时逐 Reviewer 各记一份。批次计划在开跑时写一次:全部批次的分组在任何批次完成之前就已落库,第一批尚未完成就中断的轮次也有完整的核对依据;没有计划的升级前轮次不续跑。
_Avoid_: 恢复、重试、断点续传、重跑

**轮次失败**:
一轮 Review Run 没有正常收尾的记录:一句原因挂在轮次自己上,轨迹里对应一条同样的事件。它与 Reviewer 失败分开——Reviewer 失败说的是某个模型没审成,全部模型都没审成时这一轮才算 Reviewer 失败;轮次失败说的是这一轮为什么没收好场(被重启改判、审完了没发出去),Reviewer 的结果与 Finding 照常保留。没有这条记录即收尾正常。面板的轮次列表据此分完成、运行失败(全部 Reviewer 失败)、收尾失败三态。
_Avoid_: 运行错误、异常、崩溃原因

**排空**:
服务收到停机信号之后到进程退出之间的那段状态:不再接新活——新的 webhook 投递与面板重跑一律回 503 且不开跑、不占幂等键,已开跑的轮次跑完手上那一批、把结果落库才停下;面板 API 在这段时间照常可读。排空有上限,到上限仍没停下的轮次不再等。它是续跑的上游:被排空停下的轮次没有结束时间,由下一次启动续跑。
_Avoid_: 退出中、下线、停服、关机

**只复核**:
一轮 Review Run 的一种模式:只把本审查阶段未处置的历史 Finding 复核一遍,不报新的。它只读有未处置历史的文件,复核、延续与自动处置与完整审查同一口径;零新报的那一轮不发 review。重跑默认是它,增量评审默认完整审查,两者都可在弹窗里切换;发起范围审查永远是完整审查。没有未处置历史时它不成立,那一轮不开。
_Avoid_: 复核模式、仅复核、只清历史

**仓库注册表**:
准入仓库的清单。主键是 Forge 的数值 repo id,改名与转移 owner 后凭 payload 里的 id 仍能匹配。只有注册表里的仓库的投递会被受理,未注册一律 401。
_Avoid_: 白名单、仓库列表

**Key**:
一个仓库准入 webhook 投递的凭证,每仓库一把,作为 Gitea hook 的 secret 参与 HMAC 验签。明文存库——HMAC 验签需要原始值,这是密码学约束,不是疏忽。
_Avoid_: secret、token、密码

**模型标识**:
一个 Reviewer 用的模型在本服务里的名字,形态是 `provider:model`,如 `openrouter:z-ai/glm-5.2`。Finding 与 Reviewer 的统计都按它归属来源,因此同一次审查里不得重复。带 provider 段是因为 Pi 目录里有 216 个模型 id 跨 provider 重复;分隔用冒号而非斜杠,是因为部分 model id 本身带斜杠。
_Avoid_: 模型名、model id、model

**模型服务**:
以一个 provider 标识组织的一组模型调用能力。每个模型服务对应一份目标绑定、一把模型凭据、一份自动发现的模型清单以及若干模型补录。provider 状态、凭据状态与目录状态彼此独立,一个模型是否可用由三者和它的来源共同决定。数据库中的当前模型服务版本是配置、页面读取与 Review Run 计划的唯一事实。仅被 Pi 识别、但没有凭据、模型来源或组合引用的内置 provider 是待添加来源,不属于已配置模型服务。
_Avoid_: 模型厂商、模型配置、provider 配置

**候选配置**:
操作员准备替换模型服务当前设置的一组调用端点、接口协议、模型凭据与验证模型。它只存在于当前页面,通过模型发现与真实推理后才整组生效;失败不会留下服务端草稿,也不改变当前模型服务。
_Avoid_: 草稿配置、待验证 provider

**模型发现**:
为候选配置读取可用模型清单的过程。发现成功时提供验证模型候选;发现失败或清单缺少目标模型都不表示模型服务不可用,操作员可以手填 model id,最终由真实推理决定候选配置能否生效。手填模型验证成功后同时成为该模型服务的模型补录。
_Avoid_: 模型验证、目录刷新、获取模型

**模型服务版本**:
一次成功切换后共同生效的目标绑定、模型凭据、验证记录与模型目录状态。Review Run 开始时固定使用一个模型服务版本,运行中不跟随之后的切换。
_Avoid_: provider 版本、凭据版本

**调用目标**:
调用一个模型所用的接口协议与地址(`api` + `baseUrl`)。内置服务里每个模型用自动目录中它自己那一行保存的调用目标,同一家服务可以同时含多种协议;自定义 provider 的所有模型共用操作员配置的那一个。
_Avoid_: 端点、目标地址、服务目标

**目标绑定**:
一个模型服务版本记录的调用目标集合(ADR 0027):内置服务是去重、稳定排序后的有效调用目标集合,自定义 provider 恰好一项。目标只经显式候选验证(最小真实推理)与新版本提交进入绑定;目录刷新只换目录不换绑定,刷新拉进来的新调用目标在这一版里是「调用目标未经验证」的模型,不可用。升级前没有记录集合的内置版本只延续能凭目标指纹证明的那一个调用目标,证明不了即待重新验证,模型调用前拒绝使用凭据。
_Avoid_: 目标指纹、目标集合、绑定目标

**模型凭据**:
调用一个模型服务所需的凭据,每个 provider 一把,同一模型服务下的多个模型共用。由面板写入并加密存库,主密钥来自环境变量;只写不回显,解不开的密文视为未配置(ADR 0008)。日常状态只有未配置与已验证;旧版密文缺少可信验证证据时可在升级中进入迁移专用的待重新验证状态,保留密文但不进入模型候选。它与 Key 是两类东西——Key 管仓库准入,模型凭据管模型调用。
_Avoid_: Key、API key、模型 token

**全局模型组合**:
所有未设模型覆盖的仓库共同使用的一组 Reviewer。首次配置完成后必须至少包含一个可用模型;空值只表示实例尚未完成首次配置,不表示暂停审查。
_Avoid_: 默认模型、全局 reviewers、暂停开关

**审查策略**:
实例对 Review Run 的全局默认安排,由全局模型组合、分批上限(每批改动行上限与每批文件数上限)、批次并发数与每批每模型取证上限(一个 Reviewer 在一批里最多派几次取证,默认 3)组成。各项可以独立调整,改动只影响下一轮 Review Run,已开跑的轮次沿用开跑时冻结的值;仓库只通过模型覆盖替换其中的模型组合。
_Avoid_: 全局设置、模型设置、运行配置

**审查配置就绪**:
全局模型组合非空,且其中每个模型当前都可用的实例状态。实例达到该状态后才能注册新仓库;它描述配置是否完整,不表示外部模型服务此刻一定健康。
_Avoid_: 服务健康、部署完成、已启用

**实例启用**:
实例已经审查配置就绪,并且至少注册了一个仓库的状态。首次配置检查单以此为完成点;它不表示每次 Review Run 都会成功。
_Avoid_: 审查配置就绪、服务健康、部署完成

**思考档位**:
一处模型引用上可选的一档思考投入,取值 off、minimal、low、medium、high、xhigh、max,未设即 off。它挂在引用处而不是模型上:同一个模型在全局模型组合、某个仓库的模型覆盖与一次基点探索里可以各选各的。**每个模型自己说得出它支持哪几档**:不声明推理能力的只有 off,adaptive 模型反过来没有 off(它关不掉),xhigh 与 max 由模型自己声明才有;面板只列支持的那几档,保存组合与发起探索时也只收这几档。Review Run 与基点探索开始时随所属计划一起冻结。
_Avoid_: 思考强度、推理档位、reasoning effort、thinking level

**模型覆盖**:
一个仓库对全局模型组合的替换。语义是全量替换 reviewers 列表;全局默认与每仓库覆盖同在库里,同一套判据,未设覆盖即跟随全局。注册后的下一次投递生效。
_Avoid_: 自定义模型、per-repo config

**用户**:
能登录管理面板的一个本地账号,由不可变的用户名识别,不复用 Forge 或其他外部身份。每个普通用户至多挂一个角色;没挂角色的账号照样读得到分给它的仓库,只是写与动作一律被权限格挡下(ADR 0018)。
_Avoid_: 操作员、成员、账户

**角色**:
系统管理员在面板里自定义的一组权限格。角色不预置,一个普通用户只能挂一个;角色本身不是人,也不代表系统管理员。
_Avoid_: 用户组、权限模板、管理员角色

**权限格**:
面板 API 对一类资源的一档写或动作能力,字面量形如 `repo:write`。读评审记录、仓库与处置率不由权限格决定:登录即可读,读得到哪些由仓库分配决定(ADR 0018)。角色是权限格的子集;新增权限格不会自动落到已有角色上。十一格是 `repo:write`、`review:rerun`、`review:create`、`review:complete`、`review:advance`、`finding:dispose`、`knowledge:write`、`model:read`、`model:write`、`credential:read`、`credential:write`;`model:write` 与 `credential:write` 各自包含同资源的读权限,隐含关系只剩这两对,`knowledge:write`、`review:complete`、`review:advance` 均不隐含任何其他权限。
_Avoid_: scope、能力、权限点

**系统管理员**:
专管用户与角色、并对面板其余功能始终全权限的用户类别,直接记在用户上而不进角色矩阵。零用户时用 bootstrap 口令注册的第一个用户就是系统管理员;系统允许多个,但不允许失去最后一个。
_Avoid_: admin 角色、超级用户、root

**仓库分配**:
一个用户能看见并操作的仓库集合,挂在用户上,由系统管理员在访问控制页录入。可见即可操作:分配内的仓库照角色的权限格做事,分配外的一律按不存在处理,面板回 404。系统管理员不受限,看得到全部仓库。用户删除或仓库移除时,对应的分配一并消失。
_Avoid_: 仓库权限、项目成员

**bootstrap 口令**:
零用户启动时只打印进服务日志的一次性口令,用来注册第一个系统管理员。注册成功即失效,重启换一枚,不进环境变量也不落库。
_Avoid_: admin token、初始密码、注册 token

**代次**:
一把 Key 的单调递增序号,写在 hook URL 的 `?k=` 参数上,一次列 hook 即可读出 Gitea 上装的是第几代(ADR 0007)。代次是索引不是凭证:它只决定用哪把 Key 验签,验签仍决定一切,取错即 401。代码标识符用 `generation`。
_Avoid_: 版本、key id、generation number

**远程目录**:
pi.dev 给 Pi 内置模型表做的增量。它只在操作员显式预览或刷新 Pi 内置模型服务时作为发现输入,可落入 `models-store.json` 缓存;成功发现的模型整份存进数据库目录快照。缓存不是业务真相,Review Run 不读取它也不联网。
_Avoid_: overlay、pi.dev 目录、远程增量

**厂商目录**:
某个 provider 自己公布的模型清单,只在显式模型服务发现时补远程目录仍缺的模型。同一 provider 下与远程目录或内置表撞上的模型标识,以已有那一行为准,厂商目录只补缺;发现结果随后固化进数据库目录快照。当前只接 OpenRouter。
_Avoid_: 官方目录、vendor catalog、模型列表 API

**自定义 provider**:
操作员新增的一项模型服务,接口协议限于受支持的协议清单(当前:OpenAI Completions / OpenAI Responses / Anthropic Messages)。名字由操作员起,限小写字母、数字与连字符,与 Pi 内置 provider 共用同一命名空间;新建时撞名拒收,升级迁来的既有撞名服务停用但保留全部配置。冲突服务验证新名称后,服务及全局模型组合与模型覆盖里的引用在同一事务中改名,旧冲突项随即消失。一个名字对应一个 base URL、一个接口协议与一把模型凭据;同一个端点要接两个 base URL 就起两个名字。base URL 与接口协议都是必填。
_Avoid_: 自建 provider、custom provider、私有厂商

**模型补录**:
操作员在已配置并验证凭据的 provider 下手动登记一个 model id,表示即使自动目录没有它,这个模型也应继续出现在模型清单中。它不覆盖模型信息;与自动发现的同一模型标识合成一项并同时标出两种来源。模型补录绑定该模型验证时实际用的那一个调用目标:内置服务优先取该模型可确认的调用目标(自动目录或 Pi 内置表里它自己那一行),没有时只在目标绑定恰好一项时沿用,混合协议下确定不了就拒绝并指向自定义 provider;只轮换凭据时沿用,修改调用目标(地址或协议)时必须在候选配置中逐项明确重新补录。自动目录移除模型后,补录仍使它保持可用;删除或切换时若补录是唯一来源且组合仍引用该模型,必须阻止操作并列出引用位置。
_Avoid_: 模型行、手填模型、自定义模型、model row

**迁移保留**:
升级时为旧版目标无法证明的模型行或现有模型组合自动建立的一种模型补录来源,用于保留旧配置曾依赖某个 `provider:model` 的事实,不声称知道它绑定的旧调用端点与接口协议。它与手动补录使用相同的去重、可用性、引用保护与跨目标切换规则,但来源单独显示;自动目录后来发现同一模型时两种来源并存,只有操作员显式删除才消失。
_Avoid_: 自动补录、迁移模型

**运行基线**:
模型信息没有提供运行必需字段时,MultiReviewer 为实际调用补入的一组兼容值。它只描述本系统怎样运行该模型,不属于自动发现事实:目录仍把缺失字段显示为「未提供」。当前基线是仅文本、不声明推理能力、上下文窗口 128k、最大输出 16k;可信模型信息按字段优先于基线。
_Avoid_: Pi 默认值、模型默认值、发现值

**行作者**:
一条 Finding 所在行在该轮 Review Run 的 head commit 上最后一次改动的 git author,以及那次提交。落点是本轮 diff 里的改动行时取这一行自己的判定结果;落点是 hunk 内的上下文行时取同一个 hunk 内离它最近的那处改动(同距取上方;那一处是新增就取新增行的作者,是删除就取删掉那几行的提交),这一档带**相邻改动**标记,面板在行作者之后写明——那一行本身这一轮没改,不标出来会把几个月前的提交读成这次改动的责任人。每轮 Review Run 按自己的 head 独立判定,延续过来的 Finding 不沿用上一轮的行作者。它记录 git 里的作者姓名与邮箱原样,不映射到 Forge 用户。无法判定时留空,面板显示为无法追溯。
_Avoid_: blame、责任人、提交人、归属作者

**知识条目**:
知识集里的一条记录,或为评审规则,或为项目事实,由作用范围(glob,空值即全仓库)与一句陈述构成。生命周期两态:生效与废止。
_Avoid_: 条目、entry、规则或事实

**评审规则**:
知识条目的规范型:由作用范围(glob,空值即全仓库)与一句对仓库代码「应当怎样」的规范陈述构成。违反它即构成 Finding。
_Avoid_: rule、规范、约定、评审标准

**项目事实**:
知识条目的描述型:对仓库架构、业务或环境现状的一句可核查陈述。注入后作为 Reviewer 的判断依据,本身不构成 Finding;与代码相矛盾时以代码为准。
_Avoid_: 事实、fact、架构地图、背景知识

**知识集**:
一个仓库当前生效的知识条目全体,由人经知识确认与裁决维护。空知识集是合法状态:评审照常执行,只是没有知识注入。
_Avoid_: 规则集、规则库、知识库、事实集

**知识集版本**:
一次知识确认、裁决采纳或手工增删改之后共同生效的知识集快照。Review Run 开始时固定使用一个知识集版本,运行中不跟随之后的变更。
_Avoid_: 规则集版本、规则版本、知识快照

**基点探索**:
agent 以一个基点 commit 上的仓库代码为输入推导知识条目(评审规则与项目事实)的过程,由人发起并选定所用模型,产出不设数量上限,由人批量确认。知识集尚未确认时产出知识草案;已确认后再次探索产出修订提案。
_Avoid_: 规则扫描、自动生成规则、架构分析

**知识草案**:
首次基点探索的产出,等待人逐条或批量增删改后整组确认。同一仓库只存一份,重新探索覆盖未确认的旧草案。
_Avoid_: 规则草案、草稿、待确认规则

**修订提案**:
对知识集的一条待裁决变更(新增、修改或废止,条目可为规则或事实),由基点探索或处置反哺生成,每条标注出处。只有人裁决采纳才改变知识集。重新探索取代上一次探索的待裁决提案(与知识草案同一条覆盖语义);已裁决的与处置反哺生成的不受影响。
_Avoid_: 建议、自动更新、变更请求

**处置反哺**:
带处置备注的处置触发 agent 解读备注、生成修订提案的过程:蒸馏出规范性结论提为评审规则,描述性结论提为项目事实。没有备注的处置不构成反哺输入;反哺只产生提案,从不直接修改知识集。
_Avoid_: 自动学习、规则进化、反馈闭环

**知识轨迹**:
一次基点探索或一次处置反哺里按时间顺序发生的事件序列:agent 说过的话、调过的工具与参数、提出的知识条目,以及这次任务的开始、失败与收尾。事件的形状与审查轨迹同源。轨迹随仓库保存,运行中可实时看,结束后可回看;能看这个仓库知识集的人就能看它。基点探索的轨迹挂在这个仓库最近一次探索上,处置反哺的轨迹由它产出的修订提案回溯。
_Avoid_: 规则轨迹、探索日志、agent 输出、会话记录

**裁决**:
人对一条修订提案的结论:采纳或驳回。采纳即生成新的知识集版本;提案内容可在采纳前由人修改。
_Avoid_: 审批、确认、review

**知识确认**:
人对知识草案完成增删改后使其整组生效的动作,产生该仓库的第一个知识集版本。草案为空同样确认得了,那一版就是一个空知识集。新注册仓库完成知识确认前不执行 Review Run;升级前已注册的仓库视同已确认空知识集。
_Avoid_: 规则确认、启用规则、规则初始化
